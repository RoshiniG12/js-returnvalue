// Returning a value from a function

// Define a function getMessage to return a specific message
var getMessage = function () {
    return "I’m going on an adventure!";
};

// Call getMessage and store its return value in response
var response = getMessage();

// Output the response
console.log(response);

// Define another function getMyMessage with a custom message
var getMyMessage = function () {
    return "This is my custom message!";
};

// Call getMyMessage and store its return value in myResponse
var myResponse = getMyMessage();

// Output the custom message
console.log(myResponse);



/* Further Adventures
 *
 * 1) Write a getMyMessage function
 *    that returns a message of
 *    your choosing.
 *
 * We can call functions at the console prompt.
 * Run the program, then click in the Console panel
 * and enter commands after the prompt, >
 *
 * 2) Call getMessage at the prompt.
 *    
 *    > getMessage()
 *
 * When a function is called at the prompt,
 * its return value is displayed.
 *
 * 3) Call getMyMessage at the prompt.
 *
 */